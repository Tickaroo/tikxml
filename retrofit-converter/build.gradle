apply plugin: 'java'

dependencies {
  compile project(':core')
  compile rootProject.retrofit
  testImplementation project(':processor')
  testImplementation rootProject.mockWebserver
  testImplementation rootProject.junit
}
